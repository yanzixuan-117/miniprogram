<!--pages/booking/manage.wxml-->
<view class="manage-container">
  <!-- 状态筛选 -->
  <scroll-view class="status-tabs" scroll-x enhanced show-scrollbar="{{false}}">
    <view class="tab-item {{activeStatus === '' ? 'active' : ''}}" bindtap="selectStatus" data-status="">
      全部
    </view>
    <view class="tab-item {{activeStatus === 'pending' ? 'active' : ''}}" bindtap="selectStatus" data-status="pending">
      待审核
    </view>
    <view class="tab-item {{activeStatus === 'confirmed' ? 'active' : ''}}" bindtap="selectStatus" data-status="confirmed">
      已确认
    </view>
    <view class="tab-item {{activeStatus === 'completed' ? 'active' : ''}}" bindtap="selectStatus" data-status="completed">
      已完成
    </view>
  </scroll-view>

  <!-- 预约列表 -->
  <view class="booking-list" wx:if="{{!loading && bookingList.length > 0}}">
    <view class="booking-item" wx:for="{{bookingList}}" wx:key="_id">
      <view class="booking-card" bindtap="viewDetail" data-id="{{item._id}}" data-status="{{item.status}}" data-feedback="{{item.feedback}}">
        <!-- 日期显示 -->
        <view class="booking-date">
          <view class="date-calendar {{item.status === 'pending' ? 'pending' : ''}}">
            <text class="date-month">{{item.dateText.month}}月</text>
            <text class="date-day">{{item.dateText.day}}</text>
            <text class="date-weekday">周{{item.dateText.weekday}}</text>
          </view>
          <view class="booking-time">
            <text class="time-text">{{item.startTime}}-{{item.endTime}}</text>
            <view class="status-badge status-{{item.status}}">
              {{item.statusText}}
            </view>
          </view>
        </view>

        <!-- 学员信息 -->
        <view class="booking-student">
          <image class="student-avatar" src="{{item.studentAvatar}}" mode="aspectFill"></image>
          <view class="student-info">
            <text class="student-name">{{item.studentName}}</text>
            <text class="booking-date-text">{{item.dateText.full}}</text>
            <!-- 上课人列表 -->
            <view class="students-list" wx:if="{{item.students && item.students.length > 0}}">
              <text class="students-label">上课人：</text>
              <text class="students-names">
                <text>{{item.studentName}}</text>
                <text>、</text>
                <block wx:for="{{item.students}}" wx:key="name" wx:for-index="sIdx" wx:for-item="student">
                  <text>{{student.name}}</text>
                  <text wx:if="{{sIdx < item.students.length - 1}}">、</text>
                </block>
              </text>
            </view>
          </view>
        </view>

        <!-- 备注信息 -->
        <view class="booking-note" wx:if="{{item.studentNote}}">
          <text class="note-label">备注：</text>
          <text class="note-text">{{item.studentNote}}</text>
        </view>

        <!-- 操作按钮 -->
        <view class="booking-actions">
          <!-- 待审核状态 -->
          <block wx:if="{{item.status === 'pending'}}">
            <button class="action-btn reject" catchtap="rejectBooking" data-id="{{item._id}}">拒绝</button>
            <button class="action-btn confirm" catchtap="confirmBooking" data-id="{{item._id}}">确认</button>
          </block>

          <!-- 已确认状态 -->
          <block wx:if="{{item.status === 'confirmed'}}">
            <button class="action-btn complete" catchtap="completeBooking" data-id="{{item._id}}">完成课程</button>
          </block>
        </view>
      </view>
    </view>
  </view>

  <!-- 加载中 -->
  <view class="loading-state" wx:if="{{loading}}">
    <text class="loading-text">加载中...</text>
  </view>

  <!-- 空状态 -->
  <view class="empty-state" wx:if="{{!loading && bookingList.length === 0}}">
    <text class="empty-icon">📋</text>
    <text class="empty-text">暂无预约记录</text>
    <text class="empty-hint">当有学员预约您的课程时会显示在这里</text>
  </view>
</view>
